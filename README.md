# amadeuskang.com

This is the personal website of Amadeus Kang, built in 11ty.
Files in `articles/`, compiled to static HTML in `_site/`.

## Build

```bash
npm install
npm run build   # outputs to _site/
npm run serve   # dev server with hot reload
```

Deploys serve `_site/`.
